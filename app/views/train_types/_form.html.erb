<%= form_with(model: train_type) do |form| %>
  <% if train_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(train_type.errors.count, "error") %> prohibited this train_type from being saved:</h2>

      <ul>
        <% train_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_area :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

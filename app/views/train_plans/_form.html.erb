<%= form_with(model: train_plan) do |form| %>
  <% if train_plan.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(train_plan.errors.count, "error") %> prohibited this train_plan from being saved:</h2>

      <ul>
        <% train_plan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :train_type_id, style: "display: block" %>
    <%= form.text_field :train_type_id %>
  </div>

  <div>
    <%= form.label :exercise_type_id, style: "display: block" %>
    <%= form.text_field :exercise_type_id %>
  </div>

  <div>
    <%= form.label :sequence_number, style: "display: block" %>
    <%= form.number_field :sequence_number %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
